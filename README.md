Chess Evaluation Neural Network

A neural network that evaluates chess positions — built completely from scratch in NumPy. No PyTorch, no TensorFlow, no autograd. Just the math, implemented by hand.

Give it a FEN string, get back a score for who's winning — the same idea as the eval bar you see on Lichess or Chess.com.
